import { NaturesSortPipe } from './natures-sort.pipe';

describe('NaturesSortPipe', () => {
  it('create an instance', () => {
    const pipe = new NaturesSortPipe();
    expect(pipe).toBeTruthy();
  });
});
